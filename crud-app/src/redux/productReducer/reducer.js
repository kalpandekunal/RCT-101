const initialState = {
    isLoading : false,
    isError : false,
    products : [],
}

export const reducer = (state,{type,payload})=>{
    switch(type){
        default:
            return state;
    }
}